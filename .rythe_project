local project = {
    alias = "rsl",
    namespace = "rsl",
    types = { "library" },
    additional_types = { "test" },
    dependencies = {
        "public third_party/fmt:header-only",
        "private third_party/meow_hash:header-only",
        "public third_party/semver:header-only"
    },
    defines = { "RYTHE_INTERNAL" },
    pch_file_name = "./impl/pch",
    pch_auto_generate = true,
    vector_extensions = "AVX2",
    isa_extensions = { "AES" }
}

return project
